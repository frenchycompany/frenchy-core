# =============================================================================
# Configuration FrenchyConciergerie - Fichier .env
# Copier ce fichier en .env et remplir les valeurs
# =============================================================================

# Application
APP_ENV=production
APP_DEBUG=false

# Base de données unifiée (VPS)
DB_HOST=localhost
DB_NAME=frenchyconciergerie
DB_USER=frenchy_app
DB_PASSWORD=CHANGE_ME
DB_PORT=3306
DB_CHARSET=utf8mb4

# API OpenAI (pour génération messages SMS/WhatsApp)
OPENAI_API_KEY=sk-CHANGE_ME

# Base de données SMS (même BDD que ci-dessus, en localhost sur le VPS)
# Le RPi se connecte à distance à cette même BDD
RPI_DB_HOST=localhost
RPI_DB_PORT=3306
RPI_DB_NAME=frenchyconciergerie
RPI_DB_USER=frenchy_app
RPI_DB_PASSWORD=CHANGE_ME

# Téléphone admin
ADMIN_PHONE=+33XXXXXXXXX

# Session
SESSION_TIMEOUT=1800

# Secret partagé pour authentifier les appels cron du RPi vers le VPS
# Générer avec : php -r "echo bin2hex(random_bytes(32));"
CRON_SECRET=CHANGE_ME
