# Python dependencies for SMS project scripts
# Install with: pip install -r requirements.txt

# Database
pymysql>=1.0.0

# Serial communication with GSM modem
pyserial>=3.5

# SMS via Gammu
# IMPORTANT: version pinnee a 2.12 pour correspondre a libgammu 1.41.0
# (version systeme sur le Raspberry Pi). Installer avec recompilation:
#   sudo apt install libgammu-dev python3-dev build-essential
#   pip3 install --force-reinstall --no-binary :all: python-gammu==2.12
# Ne PAS installer de wheel pre-compile : il sera lie a une libgammu plus
# recente et levera "module 'gammu' has no attribute 'GammuException'".
python-gammu==2.12

# OpenAI API (optional, for AI responses)
openai>=1.0.0
